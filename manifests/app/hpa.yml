apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: demo-todo-app
  #namespace: demo-todo
spec:
  maxReplicas: 8
  metrics:
  - resource:
      name: cpu
      target:
        averageUtilization: 70
        type: Utilization
    type: Resource
  minReplicas: 4
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: demo-todo-app
